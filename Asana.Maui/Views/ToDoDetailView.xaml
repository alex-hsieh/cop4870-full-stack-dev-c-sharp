<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Asana.Maui.Views.ToDoDetailView"
             Title="ToDoDetailView"
             NavigatedFrom="ContentPage_NavigatedFrom"
             NavigatedTo="ContentPage_NavigatedTo">
    <VerticalStackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Label Text="Id: " Grid.Column="0"/>
            <Entry Text="{Binding Model.Id}" Grid.Column="1" IsReadOnly="True"/>
            <Label Text="Name: " Grid.Column="2"/>
            <Entry Text="{Binding Model.Name}" Grid.Column="3"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Labels -->
            <Label Text="Completed?:" Grid.Row="0" Grid.Column="0"/>
            <Label Text="Priority:" Grid.Row="0" Grid.Column="2"/>

            <!-- Row 1: Controls -->
            <CheckBox IsChecked="{Binding Model.IsCompleted}" Grid.Row="1" Grid.Column="0"/>
            <Picker ItemsSource="{Binding Priorities}"
            SelectedItem="{Binding SelectedPriority}" Grid.Row="1" Grid.Column="2"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Label Text="Description: " Grid.Column="0"/>
            <Entry Text="{Binding Model.Description}" Grid.Column="1"/>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Label Text="Due Date:" Grid.Column="0"/>
            <DatePicker Date="{Binding DueDate}" Grid.Column="1"/>
        </Grid>

        <Button Text="Ok" Clicked="OkClicked" BackgroundColor="Beige"/>
        <Button Text="Cancel" Clicked="CancelClicked" BackgroundColor="Red"/>
    </VerticalStackLayout>
</ContentPage>